# ===========================================
# 自动化基金量化系统 - Python 依赖
# ===========================================
# 安装方式: pip install -r requirements.txt
# Python 版本: >= 3.10

# --- 数据源 ---
akshare>=1.14.0            # A股/基金数据（主数据源，覆盖面广）
efinance>=0.5.0            # 东方财富直接接口（备选源，更稳定，有累计净值）

# --- 数据处理 ---
pandas>=2.0.0
numpy>=1.24.0

# --- 数据库 ---
# sqlite3 是 Python 内置模块，无需安装

# --- 配置 ---
PyYAML>=6.0

# --- CLI 美化 ---
rich>=13.0.0               # 终端彩色输出、表格、进度条

# --- 可选 ---
# matplotlib>=3.7.0        # 图表
# scipy>=1.10.0            # 统计计算
schedule>=1.2.0            # 定时调度（python src/main.py schedule）
# requests>=2.28.0         # HTTP请求（如果akshare不满足需求）
