# 续作执行提示词（交接给其它智能体）

> 用途：直接粘贴给接手的智能体。**自包含**，不需要它读本次对话。
> 生成时间：2026-09-16　｜　更新：2026-09-18
> 配套文档：`docs/执行状态与续作指引.md`（状态核查）、`docs/外部参考整合方案与任务书.md`（批次 4/5 完整规格）

---

## ⚠️ 执行进度（2026-09-18）

| 步骤 | 状态 |
|---|---|
| **第 0 步 提交批次 1-3** | ✅ **已完成**（commit `a425341`，34 文件，工作区干净）—— **不要重做** |
| 任务 A `purchase_fee_rate` 接入买入路径 | ✅ **已完成**（第二批提交，317 测试全绿） |
| 任务 B 批次 4 推荐系统（10 条） | ✅ **已完成**（第二批提交，含前后对照） |
| 任务 C 批次 5.1 组合曲线 | ✅ **已完成**（第二批提交，渲染对照 + 5 个新测试） |
| 任务 D 重跑 `nav_series` 受影响报告 | ✅ **已完成**（前后对照表已交付，见 `docs/执行状态与续作指引.md` §0） |
| 黑箱验收审计 | ⬜ 未开始（不在本提示词范围） |

**Q-A / Q-B / Q-C 已由用户拍板**（见任务 B 内的「决策已定」），**不要再问，也不要自行假设**。

**接手时先读** `docs/执行状态与续作指引.md` §2（已完成，勿重做）/ §3（唯一尾巴）/ §7（铁律）。

---

## 提示词正文（从下一行开始复制）

```
你是接手 D:\DSH\projects\Quantitative-Fund-auto 这个项目的智能体。
上一个会话已经执行了任务书的前三批，但它在一次文件整理事故中丢失了记忆。
你**没有**上一轮的上下文，所以下面把必要信息全部给你。

═══════════════════════════════════════════════════════════════
第 0 步（必做，先保护现状）
═══════════════════════════════════════════════════════════════

工作区里有 565 行未提交的改动 + 2 个新源码文件 + 7 个新测试文件，
且没有任何完工报告。**这批工作已经历过一次文件丢失事故。**
在动任何代码之前，先按下面的顺序保护它。

0.1 先跑测试，确认基线是绿的：
    cd D:\DSH\projects\Quantitative-Fund-auto
    python -m pytest tests/ -q
    期望：278 passed
    若数量不是 278 或有失败 —— **停下，把实际输出贴出来问我**，不要继续。

0.2 核对将要提交的内容（不要盲目 add -A）：
    git status --porcelain
    git add -A --dry-run
    预期约 33 个文件：docs/ 7 份、src/ 13 个、tests/ 9 个。
    确认其中**没有** data/fund_quant.db、没有 .env、没有 >1MB 的文件。
    若发现异常文件 —— 停下问我。

0.3 提交：
    git add -A
    git commit -m "fix: 批次1-3（指标口径/时点隔离/业务规则）+ nav_series 估值列统一

    - 1.1 compute_metrics 的 rf_annual 单位错配（百分数 vs 小数）
    - 1.2 管理费重复计费：净值已含，management_fee_annual 默认归零
    - 1.3 前端申购费按份额类别（C/E/I 类取 0）
    - 2.1 新增 time_window 半开区间约定 + 边界测试
    - 2.2 数据缺失不得静默填充的约定固化为测试
    - 2.3 ms_to_date 显式 Asia/Shanghai 时区
    - 3.1 QDII T+2 确认（confirm_lag_for）
    - 3.2 申购状态检查（暂停申购/封闭期排除，限大额保留但标注）
    - 3.3 after_cutoff 仅在交易日生效
    - 3.4 API 层缺失数据显式声明 degraded
    - 新增 nav_series 统一估值用 acc_nav / 定价用 unit_nav"

0.4 提交后回报：
    git log --oneline -1
    git status --porcelain     # 应为空
    把这四条输出贴给我。**然后停下等我确认，不要继续。**

═══════════════════════════════════════════════════════════════
背景：项目是什么
═══════════════════════════════════════════════════════════════

中国开放式基金的量化分析系统。Python 3.13 / Flask / SQLite（data/fund_quant.db，约 150MB）。
- 数据：约 27,852 只基金的基础信息、约 1.2M 行净值、指数 PE/PB、交易日历
- 功能：市场温度计（温度→建议权益仓位）、基金质量筛选、组合持仓账务（T+1 确认 / T+2 起算 / 15:00 切点）、
  定投计划、调仓建议、板块分析、ML 波动率预测、回测、Web 前端（原生 JS SPA，无构建链）
- 真实账本：data/fund_quant.db 含用户真实持仓与流水（24 条持仓 / 28 笔流水），**是真金白银，不是测试数据**

═══════════════════════════════════════════════════════════════
铁律（违反即视为失败）
═══════════════════════════════════════════════════════════════

1. 每条修复必须附「实际运行命令 + 原始输出」。跑不了就明说跑不了，不许用"应该没问题"。
2. **写 data/fund_quant.db 之前必须先做完整文件快照**，记录前后大小与关键表行数。
   （只有需要写账本时才做；本任务的多数改动不写账本。）
3. 不许回填历史财务记录：不改历史 transactions.fee、不重算历史成本基础。
4. 不许新增运行时依赖。若认为必须，**先停下来问**。
5. 现有 278 个测试必须继续全绿（python -m pytest tests/ -q）。每项修复配新测试。
6. Web 端保持 QFA_MARKET_LIVE=0。
7. 禁止触碰：
   - src/analysis/trade_rules.py 的日期算术（resolve_apply / resolve_sell / period_dates）
   - src/data/database.py 的 _backup_rows 语义
   - src/web/static/app.js 的 esc() / stripEmoji() 调用顺序
   - src/data/collector.py 的 curl 子进程抓取方式
   - 不引入 Vite / webpack / 任何打包器
8. 脚本一律用 DB_PATH 绝对路径，不要裸调 Database()（默认路径相对 CWD，会静默建空库）。
9. **先复现，再修改**。结论与本文档不符时**停下报告**，不要按描述硬改。
10. 不得顺手扩大范围：不要重构 FundClassProfile，不要碰《数据源扩展计划书》的任何内容。
11. 分批交付，每批单独汇报。

═══════════════════════════════════════════════════════════════
已完成 —— 绝对不要重做
═══════════════════════════════════════════════════════════════

批次 1、2、3 已完整完成（含 7 个新测试文件）。以下都已验证在代码里，
**不要重做、不要"顺手改进"**：

1.1  Sharpe 单位错配        backtest.py docstring 已改为"单位小数"，有 rf_annual*100
1.2  管理费重复计费        backtest.py:57  management_fee_annual = 0.0
1.3  C 类申购费            portfolio.py:84 purchase_fee_rate()（C/E/I 取 0）
2.1  半开区间约定          src/analysis/time_window.py + tests/test_time_window.py
2.2  不得静默填充          tests/test_missing_data_policy.py
2.3  ms_to_date 时区       hithink_collector.py:32 ZoneInfo("Asia/Shanghai")
3.1  QDII T+2 确认         trade_rules.py:29 QDII_CONFIRM_LAG=2 + confirm_lag_for()
3.2  限购检查              fund_scorer.py _check_purchasable()/purchase_blocked；
                           strategy_engine 经 screen_funds() 传递性覆盖
3.3  after_cutoff 守卫     trade_rules.py:9（仅交易日生效）
3.4  API 缺失数据声明      app.py:346-352 degraded.append("cash_reserve")
3.5  分红设计方案          docs/现金分红设计方案.md
3.6  前端 esc              任务书建议不做，符合预期

另有 Claude 自主新增（不在任务书内，方向正确，保留）：
src/analysis/nav_series.py —— 统一「估值用 acc_nav / 定价用 unit_nav」，
已接入 backtest.py / fund_scorer.py / historical_recommender.py。

═══════════════════════════════════════════════════════════════
任务 A（唯一遗留的尾巴）
═══════════════════════════════════════════════════════════════

A1. 把 purchase_fee_rate 接入买入份额计算

现状：portfolio.py:84 已定义 purchase_fee_rate()，但买入路径
      portfolio.py:495 仍是 shares = round(amount / confirm_nav, 2)，
      即隐含假设「申购费 = 0」。

正确口径（来自基金页面原文，权威）：
    净申购金额 = 申购金额 / (1 + 申购费率)
    申购费用   = 申购金额 − 净申购金额
    申购份额   = 净申购金额 / T日基金份额净值

要求：
  1. 改为 net = amount / (1 + purchase_fee_rate(fund_name))；shares = round(net / nav, 2)
  2. 申购费用记入该笔 transactions.fee
  3. **C 类份额（费率 0）结果必须与现值完全一致** —— 写一条回归测试断言这一点
  4. A 类（费率 1.5%）断言 shares == round(amount/1.015/nav, 2)
  5. 用户 7/7 持仓均为 C 类，**本次改动不应改变任何现有持仓数据**

注意：不要改动 sell 路径的份额计算。

═══════════════════════════════════════════════════════════════
任务 B：批次 4 —— 推荐系统（10 条）
═══════════════════════════════════════════════════════════════

完整规格见 docs/外部参考整合方案与任务书.md 的「## 批次 4」。先读那一节。

背景（为什么这批重要）：当前「推荐」的实质是
「从 60 只代码最小的老基金里，挑 30 只回撤/动量看着还行的，顺序任意」——
因为 8 个筛选阈值里有 6 个因数据缺失失效，而排序键是常数。

10 条问题（行号已核实）：
  4.1 推荐排序是空操作      fund_scorer.py:128  sort_values(["_risk_order","mgt_fee"])
                            两个排序键都是常数（30/30 同标签 + 540/540 同费率）
  4.2 【根因】两条采集路径互相清空字段
                            database.py:425 mgt_fee=excluded.mgt_fee 无保护
                            （只有 :429 redeem_fee 有 COALESCE）
                            实测两个集合完美互斥：费率>0 且 有规模 = 0
  4.3 用已实现前向收益选 proven winners
                            historical_recommender.py:91-135（同义反复）
  4.4 债基系统性胜出        historical_recommender.py:298 dd_score 权重
  4.5 候选池 = 代码最小的 60 只
                            historical_recommender.py:233 return bond[:60]+equity[:60]
  4.6 伪月度采样            historical_recommender.py:246 timedelta(days=60)
  4.7 无风险利率硬编码 0.03 historical_recommender.py:289
  4.8 emoji 前缀做业务判定  fund_scorer.py:353 v.startswith("❌")
  4.9 两条推荐路径无协调
  4.10 回撤用全历史 / 分数范围 [5,95] / custodian_fee 0% 填充

**批次 4 内部建议顺序**（有依赖关系）：
  4.2 先做 → 4.1 才有意义（排序键 mgt_fee 现在 100% 是常数，不先修 4.2 改了也验证不出效果）
  → 4.5 / 4.6（候选池与采样）
  → 4.4（按类型分组评分）
  → 4.3 / 4.7 / 4.8 / 4.9 / 4.10

**决策已定（Q-A / Q-B / Q-C，由用户拍板，直接执行，不要再问）**：

  ┌─ Q-A：4.2 的修复范围
  │  **只防未来破坏，不重采存量 `fund_info`。**
  │  理由：重采 27,852 只需出网抓取，属《数据源扩展计划书》的范围，本批次不碰。
  │  要求：修好 upsert 保护后，把「重采存量以恢复被清空的费率/规模」单独写进
  │        `docs/执行状态与续作指引.md` 的待办清单，不要在这里顺手做。

  ├─ Q-B：4.1 的排序键
  │  **引入轻量综合评分**，排序键改为该评分。不要只换二级键。
  │  理由：只有 4 档标签 + 费率无法支撑「推荐」—— 实测 30/30 同标签、540/540 同费率，
  │        即便修好 4.2，仅按费率排序也不是好的推荐依据。
  │  要求：
  │    1. 复用**已有的** metrics（回撤 / 动量 / 夏普 / ann_vol / 规模 / 费率），
  │       加权成 0–100 分；**不引入新数据源、不需新依赖**
  │    2. **按基金类型分组归一化**（这同时解决 4.4：债基与权益的动量/回撤区间不可比）
  │    3. `mgt_fee` 只作同分时的 tie-break，且**缺失值排最后**（不得当 0 参与排序）
  │    4. 权重与归一化区间要写成**具名常量并加注释**，不要散落魔法数
  │  依赖：**必须先做完 4.2**（`mgt_fee` 现在 100% 是常数，不先修 4.2 改了也验证不出效果）

  └─ Q-C：4.3 的 proven winners
     **先只标注「样本内」，不改算法。**
     理由：改成样本外验证是独立的研究工程，容易失控，且会连带影响 4.4/4.7 的结论。
     要求：在 `historical_recommender` 的报告/返回值里**显式声明**这是样本内历史表现，
          不得让用户误读为能力证据。改算法单列为后续项。

验收要求：
  - 给出修复前后 screen_funds 输出前 30 只对照表（证明排序不再任意）
  - 给出 proven_winners / current_picks 的类型分布对照（证明不再债基一边倒）
  - 新增测试：断言 mgt_fee 缺失时排在最后；断言候选池不随 SQL 顺序变化

═══════════════════════════════════════════════════════════════
任务 C：批次 5.1 —— 组合曲线画错了量
═══════════════════════════════════════════════════════════════

**这不是渲染 bug，y 轴映射是对的。是画错了量。**

现状：src/web/static/app.js:391
    const vals=C.value, costs=C.cost;    // 画的是「市值」与「累计成本」

用户报告的症状：首页「组合累计走势」显示
    起点 2026-08-26 · 22 只持仓
    当前市值 ¥493.37 · 累计收益 ¥-8.92 · 收益率 -1.78%
但折线**涨跌都往上走**。

原因（实测数据，来自 GET /api/portfolio/curve）：
    value: [9.99, 210.3, ..., 493.37]    单调上升（因为每天在定投买入）
    cost : [10.0, 205.0, ..., 502.29]    同步上升
    pnl  : [-0.01, 5.3, ..., -8.92]      一路走低
    ret% : [-0.08, 2.58, ..., -1.78]     一路走低

两层问题：
  (a) 结构层：建仓期每日定投，市值必然单调上升，与盈亏无关
  (b) 压缩层：y 轴范围 9.99~502.29（跨度 492.3），而真实盈亏跨度仅 14.22
      → 盈亏波动只占图高 2.9%，被压成一条缝

要求（主图改画「收益率 %」）：
  1. app.js:391 → const vals = C.return_pct
  2. y 轴**关于 0 对称**：M = max(|min|,|max|)，范围取 [-M, +M]，让零轴居中
  3. app.js:405 的三条等分网格线 → 改为**过零参考线**（0% 那条加粗，是盈亏分界）
  4. app.js:406 面积填充 → 从曲线填到**零轴**，且**零轴以上绿、以下红**
     （现在按最后一点的符号给整条线上色，会导致"前期一直赚、最后亏损"时整条变红）
  5. 市值/成本从图上移除，只保留顶部文字行（建仓期市值曲线无信息量，留着误导）
  6. 图上必须标注口径：return_pct = pnl/cost 是**持仓收益率（资金加权）**，不是时间加权；
     且与「已实现收益含赎回费」是**两个不同口径**，不要混写

不要在本次做（先问用户）：
  - 叠加沪深300基准对比（需小改 get_portfolio_curve() 返回值，属新增字段）

验收：
  - 给出改动前后同一份数据的渲染对照（SVG 文本或截图）
  - 断言 return_pct 的最小/最大值在图上充满垂直空间（不再被压到 3%）
  - 断言零轴上下分别着色

═══════════════════════════════════════════════════════════════
任务 D：重跑受 nav_series 影响的报告
═══════════════════════════════════════════════════════════════

背景：Claude 上一轮新增 nav_series.py，把「估值序列」从 unit_nav 改成 acc_nav
（累 计 净值）。这会**改变所有历史报告数字**。

要求：
  1. 先明确哪些报告/输出受影响（收益、波动、回撤、动量、夏普、筛选打分、回测）
  2. 重跑它们
  3. **给出前后对照表**（关键指标变化量），不要静默换口径
  4. 若某个指标变化异常大（例如回撤变化 >30%），标出来让我判断

═══════════════════════════════════════════════════════════════
交付格式（每一批都必须）
═══════════════════════════════════════════════════════════════

批次 X — 完成

变更文件：
  - path/to/file.py:123-145  <一句话说明>

测试：
  $ python -m pytest tests/ -q
  → N passed

实测前后对比（贴原始输出）：
  修复前：...
  修复后：...

数据库快照：data/_snapshot_YYYYMMDD_HHMMSS.db（未触及 / 已修改，行数对比 ...）

未解决 / 存疑：
  - ...

需要你决策的：
  - ...

═══════════════════════════════════════════════════════════════
执行顺序与停止点
═══════════════════════════════════════════════════════════════

第 0 步 ✅ **已完成**（commit a425341）—— **跳过，不要重做**

任务 A（purchase_fee_rate）→ 【停，汇报】
任务 B（批次 4：先 4.2 → 再 4.1/Q-B 评分 → 其余）→ 【停，汇报】
任务 C（批次 5.1）→ 【停，汇报】
任务 D（重跑报告）→ 【停，汇报】
提交第二批

**不要跳过停止点连续做。** 每完成一段就停下汇报，等我确认再继续。

Q-A / Q-B / Q-C 已拍板（见任务 B），**直接按决策执行，不要再问**。

═══════════════════════════════════════════════════════════════
最后
═══════════════════════════════════════════════════════════════

- 详细状态核查见 docs/执行状态与续作指引.md（§2 已完成 / §3 尾巴 / §4 未做 / §7 铁律）
- 批次 4/5 完整规格见 docs/外部参考整合方案与任务书.md
- 注意：不要用 `git grep` 判断未跟踪文件的引用关系 —— 它默认只搜已跟踪文件，
  上一轮曾因此误判（把有测试覆盖的 time_window.py 当成"零引用"）
- 有任何与本文档不符的发现，**停下报告**，不要自行假设
```

---

## 使用说明

1. **先做第 0 步**：提交现有工作。这是最重要的——565 行改动无完工报告，再出一次文件事故就全丢。
   第 0 步做完会停下等你确认，这是个天然的检查点。
2. **Q-A / Q-B / Q-C 需你先想好**（都在任务 B 里）。如果你也没定，可以让智能体先只做 4.2，
   等看到实际数据再定 4.1 怎么改。
3. **任务 D 容易被忽略**但很重要：`nav_series` 换了估值口径，历史报告数字已经变了，
   不重跑对照就等于悄悄改了口径。
4. 黑箱验收审计（`docs/黑箱验收审计任务书.md`）**不在本提示词范围内**，建议等批次 4/5 完成后再单独一轮。
