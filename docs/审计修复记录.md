# 审计修复记录（2026-09-11）

> 本文汇总一次完整审计 → 分批修复的全过程：改了什么、实测前后数字、还剩什么没做。
> 执行依据见 `修复任务书_审计问题.md`（该文件顶部已标注"四批已执行完毕"）。
> 所有数字都由**真实执行**产生，可用文中命令重跑；没有手改过任何报告数字。

## 0. 一页速览

| 批次 | 范围 | 核心问题 | 关键实测变化 |
|---|---|---|---|
| A | 建议链计算逻辑 | 温度里 26% 权重是死常数；PE 分位被错配；调仓总资金拍脑袋 | 量能维度 **50.0 → 94°**（真读数）；温度 45.8°→56.3°；权益建议 35%→28.8% |
| B | 回测 `backtest.py` | 成本扣在没人读的变量上 → 所有指标都是费用前的 | 同网格下含费率 **45.38%** vs 零费率 **60.48%**（差 15.1pp） |
| C | ML 模块（vol/回撤/因子） | 基线与 ML 在不同样本上比；QLIKE 用错形式；回撤标签被分红污染；OOS 选型 | 四模型统一 **8445/44 月**；"ML 打败基线"结论**反转** |
| D | 重跑与文档同步 | 报告数字与文档口径全面漂移 | 5 份报告重跑；README v3 结果 +0.4%→**+7.9%** |

测试基线：**83 → 177** 个用例，`python -m pytest tests/ -q` 全绿。

## 1. 数据库安全（先说这个）

**原则**：任何写真实库的动作，先整库快照；只快照不改数；改完逐表比对。

```
快照：data/_snapshot_20260911_124134.db（153,493,504 字节）
```

四批中**唯一的真实库写入**（批次 A）：

| 写入 | 内容 | 影响面 |
|---|---|---|
| `index_daily.volume` 回填 | 15189 行（脚本 `scripts/backfill_index_volume.py --apply`） | 只补 `volume IS NULL`，从不覆盖 close/pe/pb |
| `analysis_snapshot` 新增 1 行 | `strategy_last_temp`（策略引擎的上次温度） | 新增键，不动既有键 |
| `analysis_snapshot` 刷新缓存 | `overview` / `rebalance` / `__dash__` / `sectors` | 这些端点原本缓存着 09:0x 的旧口径结果 |

**完整性比对（快照 → 现库）**：

```
holdings      24 → 24   指纹 (24, 826.03, 323.29) 一致   状态分布 holding21/pending1/sold2 一致
transactions  28 → 28   指纹 (28, 1215.0, 4.41)   一致
dca_periods   14 → 14   (14, 已挂凭证13, executed13) 一致
index_valuation 3 → 3   (3, 165.0, 110.3) 一致
fund_nav 1219610 / fund_info 27852 / trade_calendar 555  均不变
index_daily (行数, sum_close, sum_pe, sum_pb) 前后完全一致，只有 volume 0 → 15189
analysis_snapshot 5 → 6（只多了策略状态键）
```

批次 B / C / D 对真实库**零写入**（研究模块只读 DB，只写 `data/*_results/` 与 `docs/`）。

**回滚**：`copy data\_snapshot_20260911_124134.db data\fund_quant.db`

---

## 2. 批次 A — 建议链计算逻辑

这一层每天在输出**仓位建议**，所以优先级最高。

| # | 问题 | 位置 | 修复 | 实测 |
|---|---|---|---|---|
| A1 | 成交量维度恒为 50.0（`index_daily.volume` 全表 NULL，15189 行 0 个有效值）→ 温度里约 26% 权重是死数 | `collector.save_index_val_to_db` / `thermometer._calc_volume_score` | ① 采集时写入真实 volume；② 缺失维度**剔除并按剩余权重归一化**，返回 `degraded_dimensions`；③ 回填 15189 行 | `volume notnull 0 → 15189`；量能维度 `50.0 → 94°` |
| A2 | 温度→仓位是阶梯函数（39.9°→55%，40.0°→35%） | `thermometer.TEMP_LEVELS` | 档位中点之间线性插值，档位语义保留给文案 | `39.9°→45.1%` / `40.0°→45.0%`（Δ −0.1pp，原 −20pp） |
| A3 | 缺指数时 PE/PB 分位被整体减半 | `thermometer._calc_valuation_scores` | 按**实际存在的指数**权重之和归一化 | 单指数场景 28.3 → **56.6** |
| A4 | 调仓顾问总资金 = 投入 × 1.1，与计划卡 `cash_reserve=280` 打架；权益占比用成本价 | `app.py` / `rebalance_advisor.analyze` | 总资金 = 持仓**市值** + 现金弹药；权益占比改用市值 | 总资金 739.52 → **947.15**；当前权益 90.9% → **70.4%**（与计划卡一致） |
| A5 | 策略引擎 15° 调仓阈值永不触发（`_last_temp` 只在内存） | `strategy_engine` | 上次温度持久化到 `analysis_snapshot`（7 天过期） | 第二次运行："首次评估" → "**保持不动**" |
| A6 | 交易成本写死 `/10000`；且假设"把全部持仓卖一遍" | `strategy_engine._estimate_rebalance_cost` | 复用账务费率真源；只对**实际调仓成交额**计费 | 22 条持仓：¥6.56（占"仓位"0.07%）→ **¥3.38**（占成交额 0.71%）；手算复核对齐 |
| A7 | 「黄金对冲」板块几乎不可达（关键词被"周期资源"抢先命中） | `fund_boards.BOARDS` | 黄金/贵金属移到周期资源之前 | 黄金对冲 24 → **80** 只；周期资源 394 → 338；其余 14 板块不变 |
| A8 | "超跌反弹候选"只看 `ret_1m<-8 & ma_ratio>0.85`，会把**持续下跌**当反弹候选（接飞刀） | `sector_analyzer` | 按文档实现：`ret_6m>0` 且站上 120 日线；抽成可单测的纯函数 | 网络受限跑不出真实榜单，改用**对抗性单测**（持续下跌/跌破120日线均不得入选）覆盖 |
| A9 | 三处口径不一致 | `strategy_engine` / `portfolio` / `thermometer` | ① 删掉死常量 `MARKET_STATE_WEIGHTS` 并修正 docstring；② 赎回费率**单一真源** `portfolio.redeem_fee_rate`；③ `_find_index` 改 `index_code` 精确匹配 | 同一笔赎回两模块费率完全一致；`"50"` 不再命中 `中证500`（旧实现下 中证500 被忽略、上证50 被双计） |

**口径后果**：温度 45.8° → **56.3°**，建议权益仓位 35% → **28.8%**。变化来自"量能维度复活"和"指数匹配修正"两件事。

---

## 3. 批次 B — 回测 `backtest.py`

| # | 问题 | 修复 |
|---|---|---|
| B1 | 「完整交易成本」是装饰性的：成本扣在 `portfolio_value`，而入库的 `strategy_monthly` 存的是**毛收益**，`portfolio_value` 被整个丢弃 → 所有指标都不含成本 | 改为记录净收益 `(期末/期初−1)`；持仓市值按月 mark-to-market；赎回费按当期市值计；**申购费只对实际换手部分计**；`round_trip_cost` 用于报告里的成本披露 |
| B2 | 组合模拟用**同月末信号**给**同月收益**定仓位（前视） | 见下方"补充说明"——该文件是 `portfolio_simulation.py`，不在 `backtest.py` |
| B3 | 基准与策略**按位置**截断对齐（`rets[:len(sr)]`） | 改为按**日期** inner join；报告披露 `aligned_months` |
| B4 | "月度"网格是 30 天步长（12 期 = 360 天），年化有偏 | 改用自然月末（`freq="ME"`）；逐年表按自然年分组 |
| B5 | `_t_cdf` 其实是正态 CDF（`df` 参数没用）；beta 混合 ddof | 真 t 分布（scipy 可用时用 scipy，否则不完全贝塔数值实现，两者逐点对齐）；ddof 统一 |

**实测（A=修复前 / B=修复后，5 年回看，17 次调仓）**

| 指标 | A 修复前 | B 修复后 |
|---|---|---|
| 总收益 | 33.26% | 45.38% |
| 年化 | 5.91% | 7.91% |
| 年化波动 | 21.72% | 32.37% |
| 最大回撤 | 47.38% | 43.54% |
| Calmar | 0.12 | 0.18 |
| alpha vs 沪深300 | +0.647%（p=0.4599） | +1.032%（p=0.3816） |

**拆开看三种效应**（这是批次 B 最重要的一段）：

1. **成本是否真的进入指标** —— 同一自然月末网格，只切费率：
   零费率总收益 **60.48%** vs 含费率 **45.38%**（差 **−15.10pp**，年化差 −2.19pp，回撤 +4.06pp）。
   而**旧引擎带着全部费率**跑出来是 **62.99%**，和新引擎**一分钱不收**几乎一样（年化只差 0.35pp）
   —— 这就是"成本扣在了没人读的变量上"的直接证据。
2. **残留的 0.35pp** 来自 B1 的 mark-to-market 连带修正（旧版对已经漂移的组合仍用等权平均），
   第一段持有期内两者严格相等。
3. **网格修正的影响比成本还大**：同样旧引擎、同样含费率，只换网格 → 总收益 33.26% → **62.99%**，
   年化 5.91% → **10.45%**，波动 21.72% → 32.57%。基准对网格几乎不敏感（沪深300 年化 −1.34%→−1.03%），
   说明这纯粹是策略路径依赖被错误的采样日期扭曲。

**§3.1 补充：批次 B 漏做又补上的 B2**

任务书 B2 指的是 `src/analysis/portfolio_simulation.py`（与 `backtest.py` 是两个文件）。
`base_ret = port_monthly_ret.loc[m_ts]` 配 `pred_monthly[index <= m_ts]` —— 月末 m 的信号
被用在 m 月整月收益上。而且 HAR/回撤预测的目标本就是 t+1 月，`<= m` 还叠了一层期限错配。

修复：统一用 `last_signal_before()`（严格 `index < m`，恰好就是"对 m 月的预测"）；
期初从现金出发（如实收首月建仓成本）；未投资部分按无风险利率 2% 计息；选样改用 **OOS 之前** 的覆盖度。

| 方案 | 前 年化/最大回撤 | 后 年化/最大回撤 |
|---|---|---|
| Equal_Weight | 7.13% / 23.03% | 6.43% / 22.41% |
| Vol_Targeting | 5.00% / 21.18% | 4.80% / 21.69% |
| Vol_DD_Combined | 2.16% / 21.72% | 5.18% / 21.69% |

**vol-targeting 相对等权的降回撤：1.85pp → 0.72pp**（`vol_predictor` 侧的同类改动：6.64pp → 3.59pp）。
`Vol_DD_Combined` 年化从 2.16% 修到 5.18% —— 旧版"叠加回撤预警有害"的结论是前视造出来的。

---

## 4. 批次 C — ML 模块

| # | 问题 | 修复 | 实测 |
|---|---|---|---|
| C1 | 基线在全样本评分、ML 只有 OOS → 所有头对头比较都不可比 | 新增 `align_oos_window()`，把基线与 ML 强制限制到**同一 OOS 月份集合**（三个模块都改） | `n_pred/n_months` 统一为 **8445/44**（原 EWMA 19327/103、XGBoost 8339/43）；drawdown 模块 **8638**（原基线 18551 vs ML 8533） |
| C2 | QLIKE 用**波动率之比**，不是标准方差形式 | 改为 Patton(2011) `rv/h − log(rv/h) − 1`（h=预测方差）；DM 检验同步 | 数值 0.099–0.109（波动率之比）→ 0.3445–0.3693（方差之比）——量纲不同不可跨口径比 |
| C3 | 回撤标签被**分红**污染（直接在 `unit_nav` 上算，且无异常过滤） | 改用**复权净值**（优先累计净值 `acc_nav`，除息日不下挫）+ 复用异常收益过滤 | 正类率 3% **74.30%→71.75%**、5% 46.76%→43.87%、8% 20.69%→18.82% |
| C4 | 最优模型/阈值在 **OOS 测试集上挑**后当结论汇报 | OOS 切成验证段(22月)/测试段(23月)：验证段选型、测试段报告 + Bonferroni ×12 校正 + 选择偏差声明 | 选出 `RandomForest @ 8%`（验证 AUC 0.7717）→ 测试段 AUC **0.7827**、召回 70.50%；排列 p 原始 0.0196 → **校正后 0.2353，不显著** |
| C5 | 因子检验：年度表混期限；首月成本重复计；基准类型判定是恒真式 | ① 按**收益实现月**归年；② 首月按**单边**计费；③ 基准类型显式判定（白名单，判不出报 unknown） | 见下表 |
| C6 | 低危清单 L1–L6 | L1 EWMA 缺失日不再填 0 + 修注释；L2 HAR 按 Corsi 原式对齐到当月（两个模块）；L3 置换检验零假设口径写进报告；L4 组合选样改用 OOS 之前数据；L5 报告表补上 HAR-RV；L6 Sharpe 统一 rf=2% | — |

**C1 的独立性检验**（同一份面板，只切换评测区间）：

| | 全样本(104月) | OOS 子集(44月) |
|---|---|---|
| EWMA IC | 0.7728 | **0.8121** |
| 6M_Hist IC | 0.7444 | **0.7876** |

→ 变化**全部来自窗口**，不是特征。EWMA 的缺失日处理（填 0 → 跳过）单独看几乎无影响
（IC 0.7728 vs 0.7728）。

**结论反转**：修复前 XGBoost(0.7978) > EWMA(0.7711) → "ML 打败基线"；
同窗口后 **EWMA(0.8121) 最高**，XGBoost(0.8010) 最低之一 → 最简单基线最好。
`vol_model_comparison` 同向：最强基线由 HAR-RV(0.7779) 变为 **EWMA(0.8121)**，
"跑过基线"的 ML 模型从 3 个收敛到**只有 Ridge**（IC +0.0036，报告里加了"统计显著≠有实用价值"的量级提醒）。

**C5 年度表对照**（同一份月度收益序列，两种分组规则）：

| 年份 | 动量_1M 旧 | 动量_1M 新 | 动量_6M 旧 | 动量_6M 新 |
|---|---|---|---|---|
| 2015 | −21.59% | **+4.72%** | −29.89% | **−4.04%** |
| 2021 | −14.62% | −3.88% | −2.00% | +13.19% |
| 2025 | +18.52% | +6.09% | +42.17% | +20.39% |

最大错位 **26pp**。旧表把"按信号月归组的因子序列"与"按实际月末归组的基准"放在同一张表里比。
首月成本单边化：年化 +0.03~0.05pp（动量_6M −2.91% → −2.86%）。
五因子仍全部未通过天花板测试（结论不变）。

---

## 5. 批次 D — 重跑与文档同步

**重跑**（全部真实执行，未手改数字）：

| 报告 | 复现命令 |
|---|---|
| `docs/vol_prediction_report.md` | `python src/analysis/vol_predictor.py --max-funds 200` |
| `docs/vol_model_comparison_report.md` | `python src/analysis/vol_model_comparison.py` |
| `docs/drawdown_warning_report.md` | `python src/analysis/drawdown_warning.py --thresholds 3,5,8` |
| `docs/portfolio_simulation_report.md` | `python src/analysis/portfolio_simulation.py --dd-threshold 8` |
| `docs/factor_test_report.md` | `python src/analysis/factor_test.py` |
| 回测 v3 / v4 | `python src/main.py backtest3` / `backtest4` |

**v3 四条自称的处置**：

| 自称 | 处置 |
|---|---|
| 防未来函数 | **成立，保留** |
| 完整交易成本 | 修完 B1 后成立，表述收紧为"交易成本**计入指标（净收益）**" |
| 多基准对比 | 承诺的第三基准「等权基金组合」**没实现** → **删掉承诺**，并明确 alpha 只相对两个指数 |
| alpha 显著性检验 | 补上日期对齐后重新表述（t 检验走真 t 分布） |

**文档漂移修复**：

| 文档 | 改了什么 |
|---|---|
| `README.md` | 用例数 83→177、`unittest`→`pytest`、v3 结果全表换新（年化 +0.4%→+7.9%，回撤 33.2%→43.5%，alpha p=0.75→0.3816）、backtest4 三组数字换成实测、**"主动调仓降回撤"结论修正**（44.5% vs 43.5%，只差 1pp）、"6 因子加权评分"→质量筛选、温度示例换成实际输出、持仓说明改为"只读本地库"、加研究模块复现表 |
| `docs/前端优化设计方案.md` | §3.5 赎回费补"≥7 天一律为 0"+"全仓唯一真源" |
| `docs/使用指南.md` | `nav` 用途改为"质量筛选"；离线/联网温度口径重写 |
| 4 份手写 review | 顶部加**过期横幅**（逐条列出旧说法 vs 修订后实测）——正文保留为"当时的推理记录" |
| `docs/upgrade_audit_and_plan.md` | 顶部加状态回填（哪些 ✅、哪些 ⬜ 仍未做） |
| `docs/修复任务书_审计问题.md` | 顶部标注四批已执行完毕，正文"现状"字样指修复前 |

`akshare` 全库 grep：无过时说法。实测 akshare 当前可用（`fund_name_em` 27842 行、
`fund_open_fund_daily_em` 24056 行、`stock_index_pe_lg` 258 行），
所以"数据源默认 akshare / efinance 为主 akshare 为备"的描述准确 ——
早前"akshare 出网失败"是某次会话的环境现象，不是稳定事实。

---

## 6. 额外发现（不在原清单里）

1. **`index_daily.pe_percentile` 全表为 0**（采集时逐日分位留了空）。后果：vol 模块的
   `hs300_pe_pct` 特征是常数、报告里的"温度计"方案退化成**恒定 70% 仓位**。
   已用扩展窗口分位就地补算（只用 ≤ 当日数据）：
   `Thermometer avg_position 0.6999999999999997 → 0.4656`，换手 0.0097 → 0.0734。
2. **`fund_info.fund_size` 全表 98% 为空**（27302/27852）、`mgt_fee` 在长历史股票型基金里**全部为空**。
   质量筛选的这两个维度对绝大多数基金**不参与**（但会显式显示"无数据(跳过检查)"，不是静默通过）。
   之前 CLI 把缺失打印成 `费率0.00%`（读起来像零费率）→ 已改为 `费率无数据`；
   `get_pool_summary.avg_fee` 原先把缺失当 0 一起平均 → 已改为只对有数据的基金求平均。
   顺带确认 `fund_size_log` 在 vol 的 200 只里只有 7 只为空，没有造成特征行被大面积丢弃。
3. **两处报告文案错误**（不影响数字）：
   - `portfolio_simulation` 把"回撤变化 = 0"写成"回撤反升 0.00pp" → 已改为如实说明"两方案最大回撤相同"；
   - `drawdown_warning` 的排列检验标题用"最强 ML"（可能是 RandomForest）去标一个**只对 XGBoost 跑过**的
     检验 → 已改为按实际模型名标注。
4. **Web 缓存会让修复"看起来没生效"**：`/api/overview`、`/api/rebalance` 等端点缓存着
   09:0x 的旧口径快照（TTL 6h）。修复后必须刷新这些键，否则页面仍显示 45.8°。

---

## 7. 剩余项与风险

### 仍未做（已知）

| 项 | 说明 |
|---|---|
| `sector_analyzer` / `sentiment_monitor` 的模块级 `import akshare` | ✅ **已修**（2026-09-25 复核：已无模块级急切导入） |
| `RebalanceAdvisor` 内部重复跑 `screen_funds` | ✅ **已修**（2026-09-25）：改为注入预计算筛选池 + dashboard 先串行预热 funds；实测冷启动 **141s/2 次 → 82s/1 次** |
| `/api/all` 未拆成"轻量总览先出 + 分面板懒加载" | ✅ **已修**（`/api/overview` + `loadOverviewFast` 两阶段） |
| `backtest` 的「等权基金组合」基准 | 按任务书允许的路径**删掉承诺**，没实现。它能回答"选基到底有没有用"，代价是每次回测多约 5 分钟净值查询 |
| `A8` 超跌反弹候选 | 因本机申万行业数据需联网，只做了对抗性单测，**没有跑出真实榜单** |

### 风险

1. **5 个 ML 模块是 untracked 文件**，没有 git 历史。改动无法用 git 回滚，
   只能靠 `data/*/_before_*/` 里的报告与 CSV 快照恢复结果口径，代码需手工回退。
2. **审计备份散落**在 `data/vol_results/_before_*`、`data/drawdown_results/_before_*`、
   `data/portfolio_results/_before_*`，没有清理机制。
3. **`data/` 下的运行时产物未被 gitignore 完整覆盖**（见 §8.3）。

---

## 8. 仓库卫生（推送前必须处理）

### 8.1 仓库是 **public**

`origin = https://github.com/Mo12y/Quantitative-Fund-auto`，`private = false`。
`src/analysis/investment_plan.py` **已经在公开历史里**，含真实基金代码、金额、日期
（README 顶部的隐私说明讲的就是这件事）。

### 8.2 若要减少暴露面

`docs/screenshots/position.png` 等 5 张截图是**真实持仓界面**（逐只基金的市值/收益率/笔数、
现金弹药、定投计划），比 `investment_plan.py` 更直观。它们**没有被任何文档引用**，
且拍摄于批次 A 之前（`当前权益 91%` 是修复前的口径）——建议不要入库。

### 8.3 `.gitignore` 缺口

`*.db` 已忽略，但下列**未被忽略**：

```
data/*.db-wal          ← 真实库的 WAL（本次为 0 字节，但随时可能有内容）
data/*.db-shm          ← 真实库的共享内存索引
data/backups/*.json    ← _backup_rows 删行前的真实数据备份
data/cache/*.csv       ← 7.4MB 行业缓存等
data/*_results/*.csv   ← 研究输出（可重跑）
data/trade_calendar.csv
```

新增的忽略规则见 `.gitignore` 的「运行时不入库」段。
