# 参照系接入执行报告

> 执行：2026-09-22 起　｜　任务书：`docs/下一批次计划书_参照系接入与前端可见.md`
> 执行者：续作执行者（无上一轮对话上下文）
> 状态：**进行中** —— 本文件随批次推进持续更新

---

## 0. 开工前的理解与质疑（任务书要求：先写理解，再动手）

### 0.1 我对本任务的理解（3-5 行）

参照系（11,423 只 / 6 组 / `peer_pct` 外部印证 ρ=0.889）已经算对了，但它**没有任何一条通路到达用户**：
线上 `FundScreener` 用的还是拍脑袋常数（其中 `momentum_warning=40` 实测失效），`/api/recommend` 前端从未调用，
`app.js` 里一处同侪百分位都没有。所以本批次不是"再做分析"，而是**把已有的分析接上最后一公里**：
① 让推荐链路真正使用重建后的参照系（绝对概念保留绝对限值、相对概念改用组内分位）；
② 让 API 吐出组别/样本量/百分位/数据日期/样本是否充足；
③ 让用户在基金池页**看得见**「同类 P72 · A 偏股混合 · 3,278 只对照 · 截至 2026-09-18」；
④ 顺手清掉审计遗留的体验项。核心纪律是 SSOT（不重写净值口径/指标定义）与"宁可显示数据不足，也不要显示具体数字"。

### 0.2 我认为最可疑的数字：**A2 的「`fund_info.mgt_fee`（98.0% 填充）」**

任务书 §A2 要求把 `max_total_fee` 的口径改成「仅管理费」，并**按 `mgt_fee` 的实测分布重新定位阈值**，
括号里给的依据是「`fund_info.mgt_fee`（98.0% 填充）」。**实测这个 98.0% 是错的，而且错得很关键。**

```
=== 实测（python，真实库只读）===
fund_info 总数                       = 27,864
  mgt_fee          > 0 = 12,987  (46.6%)   ← 任务书说的 98.0%
  custodian_fee    > 0 =      0  ( 0.0%)   ← 与任务书一致（托管费恒为 0）
  fund_size        > 0 = 18,001  (64.6%)
  manager_tenure   > 0 = 18,675  (67.0%)
fund_profile_extra 行数              = 18,675
  fee_actual       > 0 =  9,469  (50.7%)
  peer_pct         > 0 = 18,309  (98.0%)   ← ★ 98.0% 其实属于 peer_pct，不是 mgt_fee
```

**mgt_fee 的真实分布，问题更大：**

```
有 mgt_fee = 12,987 只；min 0.01 / max 5.00
最常见取值：0.15(34.5%)  0.08(18.2%)  0.12(10.6%)  0.10(7.3%)  0.06(5.6%)  0.03(5.5%)
           0.80( 2.9%)  1.50( 1.3%)
若「仅管理费」用单一绝对阈值：
  >0.50 → 淘汰 882 只 (6.79%)      >0.80 → 319 只 (2.46%)
  >1.00 → 淘汰 254 只 (1.96%)      >1.20 → 194 只 (1.49%)
  >1.50 → 淘汰  17 只 (0.13%)      >2.00 →  10 只 (0.08%)
```

**结论（三条，全部可复现）**：

1. **98.0% 这个数是 `peer_pct` 的覆盖率，被误记到 `mgt_fee` 头上。** `mgt_fee` 只有 **46.6%**（`fee_actual` 50.7%）。
   按"实测分布"定位阈值，实际是在**不到一半的样本**上定，而另一半（53.4%）走 `⊘ 无数据` 直接跳过费率检查
   —— 这本身就是一条必须写进报告的数据缺失声明（铁律 5）。
2. **分布是双峰的**：0.15/0.08/0.12（货币、短债）与 0.80/1.50（权益）。**单一绝对阈值跨类型无意义**：
   阈值 ≤0.5 会把权益基金几乎全部淘汰（它们本来就在 1.2~1.5），阈值 ≥1.5 则形同虚设（只 0.13%）。
   这与任务书自己的 A1 原则冲突 —— A1 说"绝对概念保留绝对限值"，但费率上限**不是**绝对概念，
   它必须**按类型**（或组内分位）才能可比。**此处我建议偏离 A2 的"单一新阈值"做法**，理由如上。
3. **「该阈值永不触发」也是不准确的**：旧口径（mgt+cust）下 `total > 2.0` 实测**有 10 只**命中、
   `> 1.5` 有 17 只。准确说法是"近似失效（0.08%）"，不是"永不触发"。结论不变，但表述要改。

> 其余需要实测核验的数字（`momentum_warning` 失效程度、跨组 P81/P86/…、"常数是 P75 严格超集"）
> 我会在批次 A 用脚本逐条复测，不直接采信。

### 0.3 追加实测（更严重）：**`mgt_fee` 这个字段本身就不可信，A2 的两条路都走不通**

按组统计后，分布形状就露馅了：

| 组 | n | 中位 | P75 | P90 | P99 |
|:--|--:|--:|--:|--:|--:|
| A 偏股混合 | 3,035 | **0.15** | 0.15 | 0.15 | 1.50 |
| B 灵活配置 | 1,402 | **0.15** | 0.15 | 0.15 | 1.50 |
| C 主动股票 | 620 | **0.15** | 0.15 | 0.15 | 0.88 |
| D 指数股票 | 1,914 | 0.12 | 0.12 | 0.15 | 1.20 |
| E 债券 | 4,802 | 0.08 | 0.08 | 0.56 | 0.80 |
| F QDII/其他 | 1,152 | 0.10 | 0.15 | 0.60 | 1.50 |

**偏股混合基金的管理费中位数不可能是 0.15%**（真实水平 1.2~1.5%）。抽样核对坐实了字段是坏的：

```
000021 华夏优势增长混合     (混合型-偏股)  mgt_fee = 0.15    ← 公开管理费 1.5%
000031 华夏复兴混合A        (混合型-偏股)  mgt_fee = 0.15
000970 东方红睿元混合       (混合型-灵活)  mgt_fee = 1.50    ← 同一个字段、同一个量级，却是对的
001286 易方达新鑫混合E                    mgt_fee = None
016371 信澳业绩驱动混合C（用户持仓）        mgt_fee = 0.0     ← 用户自己持有的 8 只全是 0/None
fund_profile_extra.fee_actual / fee_source  → 均为 0.0（应是费率来源字符串，明显错位）
```

**同一个字段里混着两套量纲**（0.15 与 1.5 并存），且**用户自己持仓的基金全部无费率数据**。
因此：

- **方案 B（单一绝对阈值）**：在坏数据上定阈值 —— 阈值 1.2% 时 A 组淘汰 4.1%、E 组 0.0%；
  阈值 1.5% 时几乎全组为 0（A 组 0.2%）。**不是"跨组不一致"，是"口径根本是错的"。**
- **方案 A（组内分位）**：虽然按组归一化后 6 组淘汰率都在 2.4%~10% 之间（比 B 均匀得多，见下图），
  但**分位是坏数据的分位**：A 组 P90 = 0.15% 意味着"管理费 >0.15% 就淘汰"，会把
  **华夏优势增长这类正常权益基金整片淘汰**（它们的真实管理费 1.5%）。**分位化救不了错数据，只会把错误均匀化。**

**结论（据此调整 A2 的做法，作为对任务书的偏离）**：
A2 的正确形态不是"换口径 + 定新阈值"，而是 **三件事**：
1. **先判定费率数据不可用**：`fund_info.mgt_fee` 量纲混用、`custodian_fee` 恒 0、`fee_actual` 错位 →
   在费率检查里**如实声明"费率数据不可用"**（铁律 5：不得用假值冒充），而不是拿 0.15 当阈值；
2. **口径改为「仅管理费」仍然要做**，但**只在数据可信的子集上**生效，并显式披露覆盖率（46.6%）；
3. **数据修好之前，不给费率设阈值**（或只保留"无数据"声明）—— 否则会做出一个看似合理、
   实则把正常基金淘汰掉（或全部放行）的检查。
   数据修复属于采集侧（对齐量纲），建议单独立项。

> 本节结论**推翻了任务书 A2 的可行性前提**（不只是"98.0% 写错了"，而是"这个字段现在不能用"）。
> 按任务书 §7.1 的要求，质疑必须用实测 —— 以上全部可复现：`scripts` 级脚本见 `D:\DSH\scratch_hold\cmp_fee_options.py`
> 与 `check_mgtfee_truth.py`（临时脚本，必要时可归入 `scripts/`）。

---

## 1. 开工前的前置工作（不在任务书范围内，但先做了）

开工时发现两件**与真实账本有关**的事，按铁律 1（写库前快照）先处理：

### 1.1 09-22 到期结算（快照 → reconcile）

- 快照：`data/_snapshot_20260922_072530_pre_settle.db`
- 结果：`{'settled_buys': 2, 'settled_sells': 1}`
  - `#43` 天弘电网 → **sold**（卖出到账 ¥50.67）
  - `#75` 纳斯达克定投 → holding（4.41 份）
  - **`#77` 信澳业绩驱动 → holding（16.95 份）** —— 与 09-20 的预测完全一致
  - `#76` 仍 `pending_confirm`：**这是正确行为** —— 016453 的净值只到 2026-09-17，
    生效日 09-18 的净值未公布，按设计保持待确认，绝不猜净值
- 回滚：`copy data\_snapshot_20260922_072530_pre_settle.db data\fund_quant.db`

### 1.2 发现并修复一个**账本级 bug**（已实现收益漏计 ¥145.61）

结算后核对，发现 `#43` 的卖出**没有进入「已实现收益」**（笔数仍是 30）。追查后确认是系统性问题：

- **根因**：`reconcile()` 结算"下单时净值未公布"的买入时，**只回填持仓的 `shares`，
  没回填 `transactions.shares`**，留下 `0.0`；
  而 `get_realized_pnl()` 用 `sum(buy 流水的 shares)`，`shares <= 0 → continue` → **整只 lot 被跳过**。
- **影响面（实测）**：库里 **14 只 lot** 的买入流水 `shares=0`，其中 2 只已卖出 →
  **已实现毛额漏计 ¥145.61**（石油化工 ¥94.94 + 天弘电网 ¥50.67）；另外 12 只是"埋着的雷"，
  将来一旦卖出同样会静默消失。
- **修复**（三层，见 commit `fa8fbb8`）：
  1. **读路径**：买入流水 `shares<=0` 时按 SSOT 回退 `round(金额/确认净值, 2)` —— **不改写任何历史记录**（铁律 2）
  2. **写路径**：`reconcile()` 结算时一并回填 `transactions.shares`
  3. **护栏**：卖出成本 `min(按份额摊, 该 lot 总成本)` —— `#43` 曾因此被摊出 ¥50.84（> 实付 ¥50）造出 **−0.17 的假亏损**，实为 **+0.67**
- **复验**：已实现 30 → **32 笔**；毛额 1491.23 → **1636.84**（+145.61）；盈亏 −27.27 → **−33.08**；`#43 = +0.67`
- 新增回归测试 `tests/test_realized_pnl_legacy_buy.py`（5 例）；全测 **334 → 339 passed**

> 这条不在任务书里，但它直接关系"用户看到的钱对不对"，且属于铁律 2（账本是真实数据）的范畴，故先修并单独留痕。

---

## 2. 批次 A —— 推荐链路落改

### 2.0 新增的共用基础设施（B1 的前置，A3 依赖）

- **`src/analysis/peer_percentile.py`（新）**：把原先内嵌在 `scripts/fund_percentile.py`
  `main()` 里的「组分布 → 百分位」逻辑下沉为可复用模块，脚本与 API 共用**同一份实现**
  （SSOT 铁律）。口径全部直引 `scripts/calibrate_thresholds.py`（分组/指标/`load_navs`），
  **不重写**。提供 `build_group_arrays()` / `build_distributions()` / `threshold()` /
  `percentile()` / `describe()`，以及 B2 需要的 `insufficient_data` 显式声明。
- **`scripts/fund_percentile.py`** 改为消费该模块（本地不再定义 `DIRECTION/WEIGHT/DISPLAY_ONLY/pct_of`）。
- **参照系缓存** `data/peer_distributions.json`（运行时产物）：全库扫描 **85 秒**，
  12,440 只基金 / 6 组，全部 `full` 等级，**未映射类型 = 0**。API 走查表（毫秒级），
  缓存缺失时**不猜**（返回 None，由调用方声明"参照系未构建"）。

### 2.1 A1 绝对 vs 相对 —— ✅ 完成

写进 `fund_scorer.THRESHOLDS` 上方的注释块（改阈值前必读）：绝对概念（费率上限、
回撤容忍度）保留绝对限值；相对概念（追涨）改用组内分位；并写明依据
（常数是 P75 的严格超集 → 换分位不改变选出谁；但同一 35 在 A 组淘汰 19%、E 组 0.2%）。
由 `tests/test_batch_a_thresholds.py::TestAbsVsRelativePrinciple` 钉住。

### 2.2 A3 `momentum_warning`：常数 40 → 组内 P90 —— ✅ 完成

**实测（`python scripts/audit_batch_a.py`，n=11,440）**：

| 组 | n | 旧：>40 警告数（通过率） | 新：>组内P90 警告数（通过率） |
|:--|--:|--:|--:|
| A 偏股混合 | 3,278 | 10（0.31%） | 328（**10.0%**） |
| B 灵活配置 | 1,911 | 2（0.10%） | 191（10.0%） |
| C 主动股票 | 754 | 5（0.66%） | 76（10.1%） |
| D 指数股票 | 1,483 | 0（0.00%） | 149（10.0%） |
| E 债券 | 3,172 | 0（0.00%） | 318（10.0%） |
| F QDII/其他 | 842 | 0（0.00%） | 85（10.1%） |
| **合计** | **11,440** | **17（0.15%）** | **1,147（10.0%）** |

→ 旧常数**实测几乎不触发（0.15%）**＝检查形同虚设；新口径六组强度一致（10.0%~10.1%），
正是 A1 要的跨组一致性。**验收标准 #3「各组通过率显著低于 100%（约 90%）」达成（通过率 90.0%）**。

各组实测 P90（来自缓存）：A 3.93 / B 2.85 / C 9.42 / D 4.29 / E 0.87 / F 0.52 —— **相差 18 倍**，
这就是"同一个 40 对不同组毫无意义"的直接证据。

⚠️ 一处**必须说明的行为变化**：`momentum_warning` 从"几乎不触发"变成"每组固定 10%"，
因此界面上的 🟡注意 会变多（例如平稳上涨 6.3%/3月的基金，在 A 组会因超过 P90(3.93%) 被标追涨）。
这是任务书明确要求的结果（"不再失效"），但属于用户可感知的变化，记录在此。

### 2.3 A4 「复活」字段是否真的生效 —— ✅ 完成（结论与任务书有出入）

**实测（`scripts/audit_batch_a.py`）**：

| 字段 | 覆盖 | 驱动的阈值 | 是否真的生效 |
|:--|--:|:--|:--|
| `fund_size` | 18,001/27,864 = **64.6%** | `min_size_yi`/`max_size_yi` | ✅ **已生效**（`_check_size` 有实现） |
| `manager_tenure` | 18,675/27,864 = **67.0%** | `min_manager_years` | ❌ **不生效：该阈值全项目被引用 0 次，没有对应的检查方法** |
| `establish_date` | 550/27,864 = **2.0%** | `min_age_months` | ⚠️ 98% 的基金走 `unknown 无数据` **静默跳过**（任务书未提，同类问题） |

**规模检查的"修前/修后"证据**（有净值的 24,061 只）：`fund_size` 缺失 6,060 只（25.2%）→
走 `unknown 跳过检查`（**静默放行**）；< 0.5 亿 6,186 只（25.7%）→ 会被淘汰；> 200 亿 263 只（1.1%）→ 警告。
即：字段补齐前该检查对约 **98%** 的基金静默放行，补齐后才真正开始工作。

**与任务书的偏离（必须写明）**：任务书 A4 要求"确认 3 个「复活」字段真的生效"，但实测
`min_manager_years` **从未接线**（不只是数据饿死）—— 数据补齐也救不了它。选项有两条：
① 新增 `_check_manager`（**会给筛选引入新的一轮淘汰，是产品决策，不敢擅自添加**）；
② 如实报告它是死阈值，留待用户决定。**本次选 ②，未擅自新增检查**（遵守任务书 §7.4
"不要为了好看而加功能"）。

### 2.4 A5 `max_drawdown_1y` 保留绝对门槛 + 标注跨组位置 —— ✅ 完成

在该常量旁写入实测跨组位置（A P81 / B P86 / C P82 / D P94 / E P100 / F P96）与
"在 D/E/F 组几乎不设限（E 组等于没有回撤门槛）"，并注明"未来若要动它请按组分别论证"。

### 2.5 A6 测试 —— ✅ 完成

新增 `tests/test_batch_a_thresholds.py`（**11 例**）：组内 P90 生效、同基金不同组结论不同、
旧常数会漏判、参照系缺失时显式声明（不退回常数）、类型未映射声明、样本不足、
组别归属（含 `混合型-偏债 → E 债券` 的实测结论）、回撤仍为绝对阈值、P90 出处。

**两处既有测试的更新**（均为契约变更所致，非"为了让测试变绿"）：
- `test_momentum_surge_warns`：改用可映射类型 + 注入参照系桩（原用 `"混合型"` 这个**未映射**类型）
- `test_healthy_fund_is_green`：隔离动量维度（新语义下"平稳上涨 6.3%"在 A 组会正确触发追涨）

**全测 339 → 350 passed。**

### 2.6 A2 费率口径 —— ⏸ **押后**（用户指示：先想清有没有更好的方案）

见 §0.2/§0.3 的实测：任务书的两条路（单一绝对阈值 / 组内分位）**都建立在不可信的
`mgt_fee` 上**。已记录的三个候选方向（待最后定夺）：
1. **如实声明"费率数据不可用"**，不设阈值（最保守，符合铁律 5）；
2. 口径改「仅管理费」+ **只在可信子集上生效并披露覆盖率**（46.6%）；
3. 先修采集侧量纲（`mgt_fee` 混用两套量纲），再谈阈值 —— 属采集侧，建议单独立项。

**倾向 3 → 1 → 2 的顺序**：数据不修，任何阈值都不可信。

---

## 3. 批次 B —— 参照系接入 API

> **执行顺序与任务书不同（有意的偏离）**：**B3 →（重建参照系）→ B1/B2/B4 → B5**。
> 理由：B3 修的是**参照系入门门槛**，会改变样本量 —— 必须先改门槛、再重建分布，
> 否则 API 接进去的是"少 368 只"的旧分位。任务书把 B3 排在 B1/B2 之后，若照做会白做一遍。

### 3.1 B3 R4 定开基金门槛修正 —— ✅ 完成

**病灶**（实测）：原门槛只看**点数** ≥756，把「定开 / 低频披露」基金当成"历史不足"踢出。
典型 `000792 招商定期宝六个月期理财债券`：**日历跨度 3,374 天（≈9.2 年）却只有 591 个净值点**（半年才披露一次）。

**修法**（`scripts/calibrate_thresholds.py::iter_navs`，新增 `sparse_ok=True` 分支）：
```
点数 ≥ 756  或  （日历跨度 ≥ MIN_SPAN_YEARS(3年)  且  点数 ≥ MIN_DAYS_SPARSE(250)）
```
**为什么对定开基金用稀疏净值是正确的**：持有人本来也只能在**开放日**申赎，
两次开放之间的波动他赚不到也躲不开；用披露点算区间收益/回撤，与持有人真实体验一致。
这不是"放宽质量"，而是**修正被误排的合法样本**。

**覆盖率复算**（`python scripts/audit_coverage.py`，分母 = 数据源覆盖池 18,677）：

| 口径 | 只数 | 覆盖率 |
|:--|--:|--:|
| ① 点数 ≥756（**旧口径**） | 11,423 | **61.2%** |
| ② 跨度 ≥3 年 | 11,811 | **63.2%** |
| **B3 新口径**（① 或 ②∧点数≥250） | **11,808** | **63.2%** |

被误排的 **388 只**（audit 的 R4 类）绝大多数被救回。**验收标准 #5 达成（61.2% → 63.2%，预计约 63%）。**

**参照系重建对比**（`data/peer_distributions.json`，耗时 109 秒）：

| 组 | B3 前 n | B3 后 n | 变化 | momentum P90 |
|:--|--:|--:|--:|:--|
| A 偏股混合 | 3,278 | 3,385 | +107 | 3.93 → **3.98** |
| B 灵活配置 | 1,911 | 1,937 | +26 | 2.85 → 2.86 |
| C 主动股票 | 754 | 770 | +16 | 9.42 → 9.47 |
| D 指数股票 | 1,483 | 1,528 | +45 | 4.29 → **4.18** |
| E 债券 | 3,172 | 3,318 | +146 | 0.87 → 0.87 |
| F QDII/其他 | 842 | 870 | +28 | 0.52 → 0.52 |
| **合计** | **11,440** | **11,808** | **+368** | — |

→ 样本 +368，但**各组分位几乎不动**（最大变动 D 组 4.29→4.18）：说明 B3 救回的是
"被误排的合法样本"，**没有扭曲参照系本身**。未映射类型仍为 0。

### 3.2 B1 共用实现 —— ✅ 完成

见 §2.0：`src/analysis/peer_percentile.py` 是唯一实现，脚本与 API 共用；
`describe()` 承担 B2 需要的"样本是否充足"声明。

### 3.3 B2 样本不足必须显式声明 —— ✅ 完成

`_peer_block()`（`src/web/app.py`）对每条不足路径给出
**`percentiles: null` + `insufficient_data: true` + `reason`**：

| 情形 | reason 文案 |
|:--|:--|
| 存续 < 3 年（R3，6,856 只） | `存续不足3年（1.4 年）` ← 如实给出跨度 |
| 参照系缓存缺失 | `参照系未构建（缺 data/peer_distributions.json）` |
| 类型未映射 | `类型未映射到参照系组` |
| 该基金无可用指标 | `该基金无可用指标（缺失：…）` |
| 部分指标缺失 | `部分指标缺失，未纳入分位：…` |

**测试钉住**：`tests/test_peer_api.py::TestPeerBlock::test_never_returns_a_number_when_insufficient`
对四条不足路径逐一断言「`percentiles` 必须为 None 且必须有 reason」。
**验收标准 #4 达成。**

### 3.4 B4 接口字段 —— ✅ 完成（只增不改）

`/api/funds`、`/api/funds/board`、`/api/recommend` 的每条基金记录新增：
`group` / `group_n` / `percentiles` / `nav_asof` / `insufficient_data` / `reason`。
三者共用 `_peer_block()` 与同一份缓存，**口径一致**（不各写一遍）。
向后兼容由测试守住：`TestBoardApiBackwardCompat` 断言既有字段
（`code/name/risk/fee/momentum_3m`、`boards/total_funds/size`、`funds/summary`）一个都没少。

**参照系缓存的可获得感**：`data/peer_distributions.json` 是运行时产物（已 gitignore），
新克隆的仓库里不存在 → API 会如实返回 `参照系未构建`（而不是猜一个数）。
重建方式见 §3.6。

### 3.5 B5 `peer_pct` 回归哨兵 —— ✅ 完成

`python scripts/validate_vs_source.py`（与天天基金**独立**的同类百分位对打，11,789 只可比样本）：

| 指标（本项目的百分位） | Spearman ρ | 判定 |
|:--|--:|:--|
| **momentum_3m** | **0.888** | ★ 高相关 → 第三方印证成立 |
| max_drawdown_1y | −0.403 | 口径不同 |
| ann_vol | −0.335 | 口径不同 |
| annual_return | −0.079 | 口径不同 |
| sharpe | 0.010 | 口径不同 |

分组（用 momentum_3m）：

| 组 | 样本 | ρ | 中位差 |
|:--|--:|--:|--:|
| A 偏股混合 | 3,381 | 0.985 | 2.1 |
| B 灵活配置 | 1,928 | 0.977 | 2.7 |
| C 主动股票 | 769 | 0.994 | 1.5 |
| D 指数股票 | 1,528 | 0.940 | 3.1 |
| E 债券 | 3,314 | 0.787 | 11.1 |
| **F QDII/其他** | 869 | **0.521** | 21.9 |

- **验收标准 #6 达成**：总体 ρ = **0.888** ≥ 0.85（基线 0.889 → 改动后**无退化**）。
- 结果与任务书 §2.3 给的数字一致（A/B/C/D 0.94~0.99、E 0.79、**F 0.52**）
  → **C3 的"F 组必须降级措辞"有据可依**（F 是"其他"大杂烩，与厂商同类口径可比性弱）。
- 交叉验证：`peer_pct == (1 − rank/total) × 100` **18,328/18,328 一致**。

### 3.7 批次 B 交付与状态

| 项 | 状态 |
|:--|:--|
| B1 共用实现 | ✅ `src/analysis/peer_percentile.py`（唯一实现，脚本+API 共用） |
| B2 样本不足显式声明 | ✅ 五条不足路径全部 `percentiles:null + reason` |
| B3 R4 门槛修正 + 覆盖率复算 | ✅ 61.2% → **63.2%** |
| B4 三个接口新增字段（只增不改） | ✅ `/api/funds`、`/api/funds/board`、`/api/recommend` |
| B5 回归哨兵 | ✅ ρ = **0.888**（≥0.85） |
| 测试 | ✅ 新增 `tests/test_peer_api.py`（8 例）；全测 **350 → 358 passed** |


### 3.6 复现命令

```bash
# 1) 重建参照系分位缓存（约 110 秒）
python -c "import sys; sys.path.insert(0,'.'); from src.analysis import peer_percentile as pp; \
pp.save_cache(pp.build_distributions())"
# 2) 覆盖率复算
python scripts/audit_coverage.py
# 3) 回归哨兵（ρ 应 ≥0.85，基线 0.889）
python scripts/validate_vs_source.py
# 4) 批次 A 的阈值实测
python scripts/audit_batch_a.py
```

---

## 4. 批次 C —— 前端可见

### 4.1 C1/C2/C3 完成并**实渲染验证**（`D:\DSH\scratch_hold\C_pool.png`）

基金池页每只基金新增第二行（`poolRow` + 新增 `peerLine`/`posChip`/`qChips`），实测渲染文本：

```
020252 银河中债0-3年政金债指数A   …  同类 P— · 存续不足3年（2.7 年）      位置 —
005623 广发中债1-3年农发债指数A   …  同类 P93 · F QDII/其他 · 870 只对照 · 截至 2026-09-18  同类为大类口径
                                      位置 P80   夏普 P95   回撤 P89
001289 银华汇利灵活配置混合A      …  同类 P93 · B 灵活配置 · 1,937 只对照 · 截至 2026-09-18
                                      位置 P77   夏普 P95   回撤 P90
023232 国泰合利6个月持有混合A      …  同类 P— · 存续不足3年（1.5 年）        位置 —
```

| 项 | 要求 | 实测 |
|:--|:--|:--|
| **C1** 三要素 | 百分位 + 组别 + 对照数 + 日期 | ✅ `同类 P93 · F QDII/其他 · 870 只对照 · 截至 2026-09-18` |
| **C2** 动量只作位置 | 视觉与夏普/回撤**区分** | ✅ 动量 = **中性灰色位置条**「位置 P80」；夏普/回撤 = 纯文字 muted。原先把动量用 `--red/--yellow` 着色（会被读成"更好"）已移除 |
| **C3** F 组降级措辞 | 加限定说明 | ✅ F 组追加琥珀色「同类为大类口径」，tooltip 写明 ρ=0.52（A/B/C/D 为 0.94~0.99） |
| **B2** 继承 | 样本不足显式声明 | ✅ 页面直接显示 `同类 P— · 存续不足3年（2.7 年）` |
| DESIGN.md | 禁 emoji/渐变/重阴影、token 取自规范 | ✅ 只用 `--ink-subtle/--ink-tertiary/--surface-2/--warn`，无新增颜色 |

### 4.2 C4 `/api/recommend` —— ✅ 接进 UI（可折叠 + 强口径声明）

在池页加 `<details>`「**历史回测验证（样本内 · 仅供参考，不是推荐）**」，展开时懒加载 `/api/recommend`。
**不把它渲染成"推荐榜"**：面板首行就是警告——样本内用已实现前向收益反筛"赢家"存在同义反复、
**不构成选基能力证据**，且本项目 L1 基线也未跑赢手工权重（见 `docs/recsys_ml_report.md`）。
每条候选同样带同侪块。→ 不再有"死接口"，也没用它误导用户。

### 4.3 ⚠️ 实测暴露一个**阻塞级性能问题**（非本批次引入）

**现象**：冷启动时 `/api/all` 与 `/api/funds/board` **180 秒超时**，基金池页出不来
（暖机后正常，见 4.1 的截图）。

**cProfile 结果**（`screen_funds` 总计 **110.3 秒**）：

| 热点 | 耗时 | 说明 |
|:--|--:|:--|
| `_load_nav_series` → `sqlite3 fetchall` | **75.5 s** | 一次读回候选基金的**全部**净值行 |
| `_load_nav_series`（含 pandas 转换） | 88.0 s | 合计 |
| `get_all_fund_codes`（`SELECT DISTINCT fund_code FROM fund_nav`） | 19.7 s | 全表扫描 |

**根因（与批次 A/B 无关，已核实）**：批次 A/B 没有改动筛选器的取数路径
（`fund_scorer` 不引用 `iter_navs`/`load_navs`）。真正的变量是 **Step-1 全市场快照**：

| | 之前 | 现在 |
|:--|--:|--:|
| `fund_nav` 行数 | ~130 万 | **2,290 万** |
| 筛选候选宇宙 | ~540 | **18,617**（≥60 点的 15,483） |

→ 冷算从 ~6 秒涨到 ~110 秒，超过前端 180 秒超时。**这不是新 bug，是数据量增长把原有的
"同步全量计算"撑爆了**；`/api/sectors` 早已用"后台预热 + `status: warming` + 前端轮询"规避同类问题，
**`/api/funds` 应照抄该模式**（启动预热 `_precompute_snapshots` 已包含 `funds`，
只是首屏请求仍会同步阻塞）。

**建议**（下一批次，单独立项）：
1. `/api/funds`、`/api/funds/board`、`/api/all` 在快照未就绪时返回
   `status: "warming" + retry_in`，前端按 retry 轮询（照抄 `/api/sectors`）；
2. `_load_nav_series` 只取**近 N 行/近 1 年窗口** —— ⚠️ 注意 `_check_sharpe` 用的是**全历史**
   `np.diff(vals)`，截断会改变 sharpe/ann_vol，**必须先确认窗口口径再改**（不可想当然）；
3. `get_all_fund_codes` 的 DISTINCT 改为 `fund_info` 侧驱动或加覆盖索引。

> 本批次**未擅自修它**：它跨"取数口径/性能"两块，改错会静默改变打分（铁律 3/5 的雷区）。

### 4.5 性能问题已修（2026-09-22，用户指示优先处理）

**修了三处，全部实测复验：**

| # | 改动 | 效果（实测） |
|:--|:--|:--|
| 1 | **`get_all_fund_codes` 改为 `fund_info` 驱动 + EXISTS** | `SELECT DISTINCT …FROM fund_nav` **20.5s → 0.34s（60×）**，返回集合**完全相同**（24,061 只，已对比）。纯取数优化，零语义变化 |
| 2 | **冷启动不阻塞**：`/api/all`、`/api/funds`、`/api/funds/board` 在无可用缓存时返回 `status=warming + retry_in=15`，后台线程预热；前端按 retry 轮询（照抄 `/api/sectors`） | 冷启动三个端点 **0.00–0.03 秒返回**（原来是 180 秒超时）。修前：页面直接超时打不开 |
| 3 | **去掉重复计算**：`_compute_dashboard` 原先**直接调 `_all_funds()`**，与 `funds` 各算一遍筛选池 | 预热 **210s → 96s**（halved） |

**复验记录（冷启动 → 预热完成）**：
```
冷启动首轮:  /api/all 0.03s warming | /api/funds 0.00s warming | board 0.01s warming
+ 96s:      /api/all → source=snapshot，有数据
+168s:      /api/funds/board?size=20&limit=200 → source=cache，boards=5, total_funds=200，peer 字段齐全
```

**过程中修掉的自身缺陷（留痕）**：
- 预热 board 时按 `size=20&limit=300` 预热，而前端请求 `limit=200` → **key 不匹配 → 永远 warming**。
  已改为**按请求的实际 key 登记预热**（`_start_funds_warm(board_key=key)`）。
- 预热线程启动后才登记的 key 会被漏掉 → 改为"**循环排空待办 + 锁内原子退出**"。
- 新增 3 例测试钉住新契约（`TestColdStartWarming`：必须 <2 秒返回 warming、`fresh=1` 可绕过）。

**仍未做的深层优化（留待用户定夺）**：`_load_nav_series` 单次仍要读 ~2,290 万行净值（~75s）。
窗口化能再降一个数量级，但**会改变 sharpe 的口径**（它用全历史），必须先定"夏普按多少年算"。
本次**未动它**。

---

## 5. 批次 D —— 体验收尾

### 5.1 D1 / F-07：`init` 的"净值成功"计数改成**按实际入库行数** —— ✅ 完成

- 改动：`save_fund_nav_batch()` 现在**返回实际写入行数**；`cmd_nav()` 据此计数，并单列
  `empty_nav`（接口没抛异常但**一行都没入库**，即源头无数据）。
- 输出对比：
  ```
  旧：✅ 采集完成！净值: 600 只, ...          ← 库里只有 305 只有净值（假成功）
  新：✅ 采集完成！净值: 305 只（入库 559,402 行）, ...
      ⚠️ 另有 295 只**接口返回空、未入库任何行**（源头无净值数据）—— 它们不计入「净值成功」
  ```
- 测试：`tests/test_batch_d.py::TestF07NavCounting`（返回行数 / 空 DataFrame 返回 0 而非 None）。

### 5.2 D2 / E-04：畸形请求返回 4xx —— ✅ 完成（实测）

- 9 类错误路径改状态码：参数校验类 → **400**，服务器内部异常 → **500**；
  并顺手修掉 `未知操作: {action}` 在空 action 时拼出 **尾随空值** 的旧文案（改为显式 `(空)`）。
- **实测（HTTP 直连）**：
  ```
  POST /api/holdings {}                    -> 400  {"error":"未知操作: (空)"}
  POST /api/holdings {action:buy,amount:10} -> 400  {"error":"缺少基金代码"}
  POST /api/holdings {action:nope}          -> 400  {"error":"未知操作: nope"}
  ```
- 兼容性已确认：前端 `postJSON` 只解析 body、不检查 `r.ok`，故返 4xx 不改变前端行为。
- 测试：`TestE04HttpStatus`（6 例）。

### 5.3 D3 / E-07：端口可配（不再硬编码 5020）—— ✅ 完成（实测）

- `_resolve_port()` 优先序：`--port=N` > 第 2 个位置参数 > 环境变量 `QFA_PORT` > 5020。
  `cmd_web()` 与启动横幅也改为使用解析结果（原先横幅硬编码 5020，第二实例会指错端口）。
- **实测**：`QFA_PORT=5021 python src/main.py web` → **5021 监听成功、首页 200**，
  且 5020 未被占用 → **两个实例可以同时跑**（原 E-07 现象消失）。
- 文档同步：`README.md`、`src/main.py` 帮助。
- 测试：`TestE07PortConfig`（5 例）。

### 5.4 F-01（计划抽到本地配置）—— ⏸ 仅记录方案（时间不够，符合任务书"可只记录方案"）

现状：`total_assets` 已不再采用计划金额（§1.5 第 1 项），但**计划卡仍显示硬编码计划**。
方案（不实施）：把 `investment_plan.py` 的 `FUNDS`/`TOTAL_CAPITAL`/`CASH_RESERVE` 抽到
`config/investment_plan.local.yaml`（加进 `.gitignore`），`get_plan()` 优先读本地配置、
缺失时回落内置默认并**显式声明"用的是内置示例计划"**。README §数据与隐私已有同样建议。

### 5.5 批次 D 交付

| 项 | 状态 |
|:--|:--|
| F-07 净值计数按实际入库 | ✅ + 测试 |
| E-04 畸形请求 4xx | ✅ 实测 + 6 例测试 |
| E-07 端口可配 | ✅ 实测（5021 双实例）+ 5 例测试 |
| F-01 计划外置 | ⏸ 仅方案（已写明） |
| 测试 | ✅ 361 → **374 passed** |

---

## 6. 全批次验收对照（任务书 §五）

| # | 标准 | 状态 | 证据 |
|:--|:--|:--|:--|
| 1 | 334+ 测试全绿 | ✅ **374** | `python -m pytest tests/ -q` |
| 2 | 费率口径已修且**有测试** | ⏸ **未做（A2 押后）** | 实测 `mgt_fee` 字段本身不可信，见 §0.3 |
| 3 | `momentum_warning` 分位化且不再失效 | ✅ | 警告率 0.15% → **10.0%**，六组一致（§2.2） |
| 4 | 样本不足显式声明 | ✅ | 五条路径 + `test_never_returns_a_number_when_insufficient`（§3.3） |
| 5 | R4 门槛修正后覆盖率复算 | ✅ | 61.2% → **63.2%**（§3.1） |
| 6 | 回归哨兵 ρ ≥ 0.85 | ✅ | ρ = **0.888**（§3.5） |
| 7 | 前端可见且可截图 | ✅ | 池页出现「同类 P93 · B 灵活配置 · 1,937 只对照 · 截至 2026-09-18」，截图 `D:\DSH\scratch_hold\C_pool.png`（§4.1） |
| 8 | 无新增运行时依赖 | ✅ | 未加三方库；前端仍是 vanilla JS、无构建链 |
| 9 | 铁律未被破坏 | ✅ | 写库前有快照；账本未污染（holdings 57 / transactions 89 全程未变） |

**唯一未达标项是 #2（A2 费率口径）**，原因是实测发现其前提不成立（字段本身量纲混用），
按用户指示押后。**这不是"没做"，是"做了调查后发现原方案不可行"**，详见 §0.2/§0.3。

---

## 7. 遗留清单（供下一批次）

| # | 事项 | 优先级 | 说明 |
|:--|:--|:--|:--|
| 1 | **A2 费率口径** | 高 | 先修采集侧 `mgt_fee` 量纲（同字段混 0.15/1.5 两套），否则任何阈值都不可信 |
| 2 | `_load_nav_series` 窗口化 | 中 | 可再降一个数量级；⚠️ 会改变 sharpe（现用全历史），**先定"夏普按几年算"** |
| 3 | `指数型-固收` 分组归属 | 中 | 现归 F（→ 被打"可比性弱"标签）；用分位网格实测评估是否移入 E |
| 4 | F-01 计划外置 | 中 | 方案已写（§5.4） |
| 5 | 组合曲线叠加沪深300基准 | 低 | 早先 §9 待办，本批次未动 |
| 6 | `select_weights` 等历史遗留 | 低 | 见 `执行状态与续作指引` §9 |

### 4.4 一处观察（未改，留给用户判断）

`指数型-固收`（如 `020252 银河中债0-3年政金债指数A`）在 SSOT `TYPE_GROUPS` 里归 **F QDII/其他**，
于是这些**债券指数基金**被打上「同类为大类口径（可比性弱）」。看着别扭，但**分组是 SSOT、
归属由上一批次的分位网格实测决定**，本批次**不擅自改映射**（改了会连带改变参照系）。
建议下一批次用同样的方法（实测分位网格）评估是否把 `指数型-固收` 移入 E 债券。


